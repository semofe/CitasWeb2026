{
  "name": "citasweb2026",
  "version": "1.0.0",
  "description": "Sistema de gestión de citas médicas",
  "license": "ISC",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm -C backend run dev\" \"npm -C frontend run dev\"",
    "dev:backend": "npm -C backend run dev",
    "dev:frontend": "npm -C frontend run dev",
    "build": "npm -C frontend run build",
    "build:full": "npm -C frontend run build && echo 'Build completado'",
    "start": "npm -C backend start",
    "preview": "npm -C frontend run preview"
  },
  "devDependencies": {
    "concurrently": "^9.2.3"
  },
  "dependencies": {
    "nodemailer": "^9.0.1"
  }
}
